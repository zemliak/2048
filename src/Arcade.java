import java.awt.GridLayout;

import javax.swing.JButton;
import javax.swing.JFrame;

public class Arcade {
	public static void main(String[] args) {
		 Game g = new Game();

		/*
		 * int[][] x = new int[3][3]; for (int i = 0; i < 3; i++) { for (int j =
		 * 0; j < 3; j++) { x[i][j] = i; } } for (int[] i : x) { for (int j : i)
		 * { System.out.print(j + " "); } System.out.println(); }
		 */
		/*
		 * JFrame frame = new JFrame(); frame.setLayout(new GridLayout(4,4));
		 * for(int i = 0; i < 16; i++){ JButton b = new JButton(i+"");
		 * frame.add(b); } frame.setVisible(true); frame.setSize(400,400);
		 */

	}
}
